/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['estateon.com'], // Add your local domain here
        formats: ["image/webp"],
      },
}

module.exports = nextConfig
